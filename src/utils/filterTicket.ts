import React from 'react'
import { TTickets } from '../types/typeTickets'

const filterTicket = (data: TTickets[], key: string) => {
  console.log(data)
  console.log(key)
  return ""
}

export default filterTicket